<% @post = post %>
<li id="post-<%= @post.id %>" class="post">
  <%= profile_img_for @post.author %>
  <%= link_to @post.author.name.full, profile_path(@post.author), class: :author %>
  <%= content_tag(:div, "Posted #{ time_ago_in_words(@post.created_at) } ago.",
    class: :timestamp) %>
  <div class="content">
    <%= @post.content %>
  </div>
  <% if current_user.writing_post?(@post) %>
    <%= link_to 'Delete post', post_path(@post), method: :delete %>
  <% end %>
</li>
