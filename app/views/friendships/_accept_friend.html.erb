<%= form_for(friend.friendships.find_by(friend_id: current_user.id),
             html: { method: :patch } ) do |f| %>
  <%= hidden_field_tag :user_id, friend.id %>
  <%= f.submit I18n.t('customs.buttons.accept_friend'), class: "btn btn-selectable" %>
<% end %>
