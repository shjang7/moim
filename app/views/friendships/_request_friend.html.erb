<%= form_for(current_user.friendships.build) do |f| %>
  <div><%= hidden_field_tag :friend_id, friend.id %></div>
  <div><%= hidden_field_tag :type, "request" %></div>
  <%= f.submit I18n.t('customs.buttons.request_friend'), class: "btn btn-selectable" %>
<% end %>
