# frozen_string_literal: true

# before(:all) do
# @request.env['devise.mapping'] = Devise.mappings[:user]
# sign_in jen
# end

# it 'redirects when giving wrong id' do
#   jen.id = nil
#   get :show, params: { id: nil }
# expect(response).to have_http_status(:success)
# end

# it 'should '
